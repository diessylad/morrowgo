export const metadata = {
  title: 'Your order | MORROWGO',
  robots: { index: false, follow: false },
  referrer: 'no-referrer'
};

export default function OrderLayout({ children }) {
  return children;
}
